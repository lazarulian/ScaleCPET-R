# Cosmed Patient Demographics

# demographics_data
list(
  last_name <- cleaned_data()$last_name[1],
  first_name <- cleaned_data()$first_name[1],
  sex <- cleaned_data()$sex[1],
  age <- cleaned_data()$age[1],
  height <- cleaned_data()$height[1],
  weight <- cleaned_data()$weight[1],
  id <- cleaned_data()$id[1],
  date_of_study <- cleaned_data()$date_of_study[1]
  
)